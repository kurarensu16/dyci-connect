server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback so /signup, /student/dashboard, etc. all hit index.html
    location / {
        try_files $uri /index.html;
    }
}